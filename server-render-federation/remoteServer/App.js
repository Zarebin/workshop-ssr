import React from "react";
import SharedComponent from "./SharedComponent";

const App = () => (
  <div style={{backgroundColor: "yellow"}}>
    <h1>Remote Server</h1>
    <SharedComponent />
  </div>
);

export default App;
